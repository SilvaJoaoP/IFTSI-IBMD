# URL de conexão do seu banco de dados PostgreSQL
# Exemplo: postgresql://USUARIO:SENHA@HOST:PORTA/NOMEDOBANCO
DATABASE_URL=""

# Chave secreta para criptografar as sessões do Next-Auth
# Gere uma usando: openssl rand -base64 32
AUTH_SECRET=""